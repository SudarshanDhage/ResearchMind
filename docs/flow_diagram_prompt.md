# Cursor Image Generation Prompt

Use this prompt in Cursor image creation:

A clean academic system architecture flow diagram for a final year project titled "ResearchMind". Professional white background, neat rectangular boxes with thin borders, blue and orange accent colors, suitable for an IEEE-style report and presentation. Show a left-to-right pipeline with arrows: User -> React Frontend -> FastAPI Backend -> Search Agent (Tavily API) -> Reader Agent (BeautifulSoup Web Scraper) -> TextCNN Source Quality Filter (Local PyTorch Model) -> Writer Agent (LLM Report Generation) -> Critic Agent (LLM Evaluation) -> SQLite Database and Final Report Output. Add a small annotation near TextCNN: "Original contribution: locally trained CNN filters reliable vs unreliable sources". Keep text readable, minimal icons, no clutter, formal engineering style.

Suggested figure caption:
Figure 1. Overall architecture of ResearchMind showing the multi-agent workflow and the locally trained TextCNN source-reliability module.
