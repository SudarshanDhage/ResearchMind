# ResearchMind frontend

React + Vite UI for the ResearchMind API.

```bash
npm install
npm run dev
```

The UI expects the FastAPI backend at `http://localhost:8000`.
Override with `VITE_API_URL` if needed.
